# Frontend environment variables for Next.js
# The orchestrator will populate the real .env. Do not commit secrets.

# URL of the unified_connector_backend (FastAPI)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
