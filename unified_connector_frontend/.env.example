# Frontend environment variables (copy to .env.local during development)
# PUBLIC_INTERFACE
# NEXT_PUBLIC_API_BASE_URL is the base URL for the FastAPI backend that the Next.js app will call from the browser.
# Example values:
# - http://localhost:8000
# - https://your-backend.example.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
